{
  "id": "dateien_module",
  "titel": "Kapitel 16: Dateien, Module und erste eigene Projekte",
  "einleitung": "Jetzt wird es richtig praktisch. Echte Programme lesen und schreiben Dateien. Sie nutzen Module – das sind fertige Bausteine aus Code. Und sie wachsen zu kleinen Projekten. Am Ende baust du dein erstes eigenes Skript. Ein Skript ist ein kleines Programm. Du schaffst das – in Python und in C++.",
  "abschnitte": [
    {
      "titel": "Dateien lesen",
      "python": {
        "text": "Fast jedes echte Skript arbeitet mit Dateien. In Python öffnest du eine Datei mit open(). Nutze den with-Block: Er schließt die Datei automatisch. Das ist wie beim Heimkommen – die Tür fällt hinter dir zu. read() liest den ganzen Inhalt. readlines() gibt alle Zeilen als Liste zurück. readline() liest nur eine Zeile. Fehlt die Datei, wirft Python einen Fehler: FileNotFoundError. Fange ihn mit try/except ab. Das ist dein Sicherheitsnetz. Achtung: In Dateien steht Text. Brauchst du Zahlen, wandle sie um: int() oder float().",
        "code": "# daten.txt einlesen\nwith open(\"daten.txt\", encoding=\"utf-8\") as datei:\n    inhalt = datei.read()          # komplett als String\n    zeilen = datei.readlines()     # als Liste (nach read leer!)\n\n# Zeile für Zeile (speicherschonend bei großen Dateien)\nwith open(\"daten.txt\", encoding=\"utf-8\") as datei:\n    for zeile in datei:\n        print(zeile.strip())       # strip() entfernt \\n\n\n# Fehler abfangen:\ntry:\n    with open(\"fehlt.txt\") as f:\n        print(f.read())\nexcept FileNotFoundError:\n    print(\"Datei existiert nicht!\")"
      },
      "cpp": {
        "text": "In C++ liest du Dateien mit std::ifstream. Das ist ein Eingabe-Strom aus der Datei. Stell dir einen Wasserhahn vor: C++ zapft Zeichen für Zeichen an. Du brauchst die Bibliothek <fstream>. Nach dem Öffnen prüfst du, ob es geklappt hat. Fehlt die Datei, benimmt sich ifstream wie false. Zeilen liest du mit std::getline. Am Dateiende liefert der Strom false. Perfekt für die while-Schleife: Sie läuft bis zum Ende. Zahlen liest du mit >>. C++ wandelt sie automatisch um.",
        "code": "#include <fstream>\n#include <iostream>\n#include <string>\n\nstd::ifstream datei(\"daten.txt\");\nif (!datei) {                  // Öffnen fehlgeschlagen?\n    std::cerr << \"Datei fehlt!\" << std::endl;\n    return 1;\n}\n\nstd::string zeile;\nwhile (std::getline(datei, zeile)) {  // bis Dateiende\n    std::cout << zeile << std::endl;\n}\n\n// Zahlen lesen:\nint zahl;\nwhile (datei >> zahl) {\n    std::cout << zahl * 2 << std::endl;\n}\n// datei schließt sich selbst (RAII)!"
      },
      "vergleich": "Python: with open() mit read oder readlines. Die Datei schließt sich automatisch. C++: ifstream mit getline. Prüfe mit if (!datei). RAII schließt die Datei selbst. RAII heißt: Das Objekt räumt von allein auf.",
      "merk": "Immer prüfen, ob die Datei da ist. Python: try/except FileNotFoundError. C++: if (!datei)."
    },
    {
      "titel": "Dateien schreiben",
      "python": {
        "text": "Zum Schreiben öffnest du die Datei mit open(datei, \"w\"). w steht für write – also schreiben. Achtung: w überschreibt den alten Inhalt. a steht für append – also anhängen. write() schreibt Text in die Datei. Mit print(datei=...) druckst du direkt in die Datei. Wichtig: Bei w ist der alte Inhalt SOFORT weg – schon beim Öffnen! Sichere also zuerst wichtige Daten. Vergiss die Kodierung nicht: encoding=\"utf-8\". Sonst werden Umlaute wie ä oder ö falsch gespeichert.",
        "code": "# Schreiben (überschreibt!)\nwith open(\"ausgabe.txt\", \"w\", encoding=\"utf-8\") as datei:\n    datei.write(\"Zeile 1\\n\")\n    datei.write(f\"Summe: {3 + 4}\\n\")\n\n# Anhängen (append)\nwith open(\"log.txt\", \"a\", encoding=\"utf-8\") as log:\n    log.write(\"Neuer Eintrag\\n\")\n\n# Mehrere Zeilen aus einer Liste\nzeilen = [\"a\", \"b\", \"c\"]\nwith open(\"liste.txt\", \"w\", encoding=\"utf-8\") as datei:\n    for zeile in zeilen:\n        datei.write(zeile + \"\\n\")"
      },
      "cpp": {
        "text": "Zum Schreiben nutzt du std::ofstream. Das ist der Ausgabe-Strom für Dateien. ofstream datei(\"ausgabe.txt\") überschreibt die Datei. Mit std::ios::app hängst du an. Der Operator << ist der Schreib-Pfeil. Er funktioniert wie bei std::cout – du kannst alles schreiben. Prüfe auch hier nach dem Öffnen: if (!datei). Existiert der Ordner nicht, schlägt das Schreiben fehl. Am Ende: close() – oder RAII macht es für dich.",
        "code": "#include <fstream>\n#include <iostream>\n\n// Schreiben (überschreibt!)\nstd::ofstream datei(\"ausgabe.txt\");\nif (!datei) {\n    std::cerr << \"Kann nicht schreiben!\" << std::endl;\n    return 1;\n}\ndatei << \"Zeile 1\" << std::endl;\ndatei << \"Summe: \" << (3 + 4) << std::endl;\ndatei.close();\n\n// Anhängen:\nstd::ofstream log(\"log.txt\", std::ios::app);\nlog << \"Neuer Eintrag\" << std::endl;\n// RAII: log schließt sich am Ende des Blocks"
      },
      "vergleich": "Python: \"w\" überschreibt, \"a\" hängt an. C++: ofstream überschreibt, mit ios::app hängt es an. Beide Sprachen: Nach dem Öffnen prüfen!",
      "merk": "\"w\" und ofstream überschreiben sofort. Bei wichtigen Daten: erst sichern!"
    },
    {
      "titel": "Module und Imports – Code teilen",
      "python": {
        "text": "Python teilt Code in Module auf. Ein Modul ist eine .py-Datei. Denke an Legosteine: Du baust Großes aus kleinen Teilen. import math lädt die Standard-Bibliothek. Das sind fertige Werkzeuge von Python. from datetime import datetime holt gezielt einen Namen. Eigene Module? import hilfsfunktionen lädt deine zweite Datei. Wichtig: Code außerhalb von Funktionen läuft beim Import sofort mit. Schütze ihn mit if __name__ == \"__main__\":. Dann startet er nur beim direkten Aufruf.",
        "code": "# hilfsfunktionen.py\n\ndef verdopple(x):\n    return x * 2\n\n# Nur beim DIREKTEN Start ausführen, nicht beim Import:\nif __name__ == \"__main__\":\n    print(verdopple(21))   # 42\n\n# main.py\nimport hilfsfunktionen\nfrom math import sqrt\n\nprint(hilfsfunktionen.verdopple(5))   # 10\nprint(sqrt(16))                       # 4.0"
      },
      "cpp": {
        "text": "In C++ teilst du Code über Header-Dateien. Eine .h-Datei ist wie ein Inhaltsverzeichnis. Sie enthält die Deklarationen – die Prototypen. Eine Deklaration sagt: Diese Funktion gibt es. Die .cpp-Datei enthält die Definition. Die Definition zeigt, wie die Funktion arbeitet. #include \"mein_modul.h\" bindet die Datei ein. Anführungszeichen: eigene Datei. <...>: Standard-Bibliothek. #pragma once verhindert doppeltes Einbinden. Es ist wie ein Schild: Schon drin gewesen! So bleibt das Programm ordentlich – keine Monster-Datei.",
        "code": "// mein_modul.h\n#pragma once            // nur einmal einbinden\nint verdopple(int x);   // Deklaration (Prototyp)\n\n// mein_modul.cpp\n#include \"mein_modul.h\"\nint verdopple(int x) {  // Definition\n    return x * 2;\n}\n\n// main.cpp\n#include <iostream>\n#include \"mein_modul.h\"\n\nint main() {\n    std::cout << verdopple(5);   // 10\n    return 0;\n}\n// Kompilieren: g++ main.cpp mein_modul.cpp -o programm"
      },
      "vergleich": "Python: import Modul oder from Modul import name. Dazu der __name__-Guard. C++: #include mit Header-Dateien und #pragma once. Deklaration und Definition sind getrennt.",
      "merk": "Python: der __name__-Guard. C++: #pragma once und Prototypen im Header."
    },
    {
      "titel": "Dein erstes eigenes Projekt – der Fahrplan",
      "python": {
        "text": "Jetzt hast du alles für dein erstes echtes Skript. Ein Skript ist ein kleines Programm. So sieht der Fahrplan aus: 1. Verstehe die Aufgabe. Was soll das Programm tun? 2. Definiere die Eingaben: Tastatur, Datei, Argumente. 3. Zerlege die Logik in kleine Funktionen. 4. Gestalte die Ausgabe. 5. Denke an Fehler: Datei fehlt, falsche Eingabe. Starte klein! Zum Beispiel mit einem Notiz-Skript. Es liest eine Datei, fügt eine Zeile hinzu und speichert sie. Damit übst du Lesen, Schreiben, Funktionen und Schleifen – in einem echten Projekt.",
        "code": "# notiz.py – dein erstes kleines Projekt\nimport sys\n\ndef zeige_notizen():\n    try:\n        with open(\"notizen.txt\", encoding=\"utf-8\") as f:\n            print(f.read() or \"(leer)\")\n    except FileNotFoundError:\n        print(\"(noch keine Notizen)\")\n\ndef notiz_hinzufuegen(text):\n    with open(\"notizen.txt\", \"a\", encoding=\"utf-8\") as f:\n        f.write(text + \"\\n\")\n    print(\"Gespeichert!\")\n\nif __name__ == \"__main__\":\n    if len(sys.argv) > 1:\n        notiz_hinzufuegen(\" \".join(sys.argv[1:]))\n    else:\n        zeige_notizen()\n# Aufruf: python3 notiz.py \"Meine erste Notiz\""
      },
      "cpp": {
        "text": "In C++ ist der Fahrplan derselbe. Dazu kommt der Build-Schritt. Build heißt: Code in ein Programm übersetzen. 1. Verstehe das Problem. 2. Plane die Struktur: main, Funktionen, Header. 3. Schreibe den Code. 4. Übersetze ihn mit -Wall -Wextra. Keine Warnungen? Fertig! 5. Teste das Programm. Ein gutes erstes Projekt: ein Notiz-Programm mit Dateien. Teile es auf: main.cpp, notizen.h, notizen.cpp. So übst du Header, Funktionen, Datei-I/O und den ganzen Build-Prozess. Datei-I/O heißt: Dateien lesen und schreiben.",
        "code": "// main.cpp – dein erstes kleines Projekt\n#include <iostream>\n#include <fstream>\n\nvoid zeige_notizen() {\n    std::ifstream datei(\"notizen.txt\");\n    if (!datei) { std::cout << \"(noch keine Notizen)\"; return; }\n    std::string zeile;\n    while (std::getline(datei, zeile))\n        std::cout << zeile << std::endl;\n}\n\nint main(int argc, char* argv[]) {\n    if (argc > 1) {\n        std::ofstream datei(\"notizen.txt\", std::ios::app);\n        datei << argv[1] << std::endl;\n        std::cout << \"Gespeichert!\" << std::endl;\n    } else {\n        zeige_notizen();\n    }\n    return 0;\n}\n// g++ -std=c++17 -Wall -Wextra main.cpp -o notiz && ./notiz"
      },
      "vergleich": "Der Fahrplan ist in beiden Sprachen gleich: verstehen, planen, bauen, testen. Python: python3 notiz.py. C++: g++ -Wall -Wextra und dann ausführen.",
      "merk": "Erstes Projekt: eine Notiz-App mit Dateien. Sie deckt Lesen, Schreiben, Funktionen, Fehler und den Build ab."
    }
  ]
}
